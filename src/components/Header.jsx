import React from 'react';

const Header = () => {
  const headerStyle = {
    textAlign: 'center',
    padding: '20px',
    color: '#FFD699', // Text color
    fontWeight: 'bold',
    fontSize: '24px',
  };

  return (
    <div style={headerStyle}>IMAGE GALLERY</div>
  );
}

export default Header;
